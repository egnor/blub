# Registers this directory as a Zephyr module so hooks defined in
# sysbuild/CMakeLists.txt are called by the build and can tweak settings.
name: cell_modem_firmware
build:
  depends: [nrf] # ensure our hooks run after Nordic's hooks
  sysbuild-cmake: sysbuild
